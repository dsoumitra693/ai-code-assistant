import { CounterClockwiseClockIcon, Cross2Icon, PlusIcon } from "@radix-ui/react-icons";

export const ICONS = {
    "cross": Cross2Icon,
    "history": CounterClockwiseClockIcon,
    "plus":PlusIcon
}